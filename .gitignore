# Env
.env
.env.local

# Data locale (images locales, cache seen_urls)
data/

# Gmail OAuth — TOUJOURS ignorés dans ce starter (repo public sur GitHub).
# Chaque utilisateur génère ses propres tokens en local via setup_gmail.py
# puis les commit dans SON propre repo PRIVÉ pour que Railway puisse s'en servir.
config/gmail_token.json
config/gmail_credentials.json
config/client_secret_*.json

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
